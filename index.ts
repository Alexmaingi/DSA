// function chunkArr(arr: number[], size: number) {
//   const chunk: number[] = [];
//   for (let i = 0; i < arr.length; i += size) {
//     chunk.push(arr.slice(i, i + size));
//   }
//   return chunk;
// }

// console.log(chunkArr([1, 2, 3, 3, 4, 5, 6, 7], 2));

// class NewChunk {
//   chunkArr(arr: number[], size: number) {
//     const chunk: any = [];
//     for (let i = 0; i < arr.length; i += size) {
//       chunk.push(arr.slice(i, i + size));
//     }
//   }
// }
